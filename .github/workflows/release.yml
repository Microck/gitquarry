name: Release

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  verify-release:
    name: Verify release tag
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Verify tag matches package version
        run: |
          set -euo pipefail
          PACKAGE_VERSION="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "gitquarry") | .version')"
          EXPECTED_TAG="v${PACKAGE_VERSION}"
          if [ "${GITHUB_REF_NAME}" != "${EXPECTED_TAG}" ]; then
            echo "tag ${GITHUB_REF_NAME} does not match package version ${EXPECTED_TAG}" >&2
            exit 1
          fi

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test
        run: cargo test --locked

      - name: Package
        run: cargo package --locked --allow-dirty

      - name: CLI smoke
        run: |
          cargo run --locked -- --help
          cargo run --locked -- search --help
          cargo run --locked -- auth login --help

  publish-crate:
    name: Publish crate
    needs: verify-release
    runs-on: ubuntu-latest
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Check publish token availability
        id: token-check
        run: |
          if [ -n "${CARGO_REGISTRY_TOKEN:-}" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Explain skipped crate publish
        if: steps.token-check.outputs.has_token != 'true'
        run: echo "CARGO_REGISTRY_TOKEN is not configured; skipping crates.io publish."

      - name: Publish to crates.io
        if: steps.token-check.outputs.has_token == 'true'
        run: cargo publish --locked --token "${CARGO_REGISTRY_TOKEN}"

  build-release:
    name: Build release (${{ matrix.target }})
    needs: verify-release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: gitquarry
            archive: tar.gz
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary: gitquarry
            archive: tar.gz
            linker: aarch64-linux-gnu-gcc
          - os: macos-13
            target: x86_64-apple-darwin
            binary: gitquarry
            archive: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: gitquarry
            archive: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: gitquarry.exe
            archive: zip
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Install Linux cross toolchain
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build release binary
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: ${{ matrix.linker || '' }}
        run: cargo build --locked --release --target ${{ matrix.target }}

      - name: Compute asset name
        id: asset
        shell: bash
        run: echo "name=gitquarry-${GITHUB_REF_NAME}-${{ matrix.target }}.${{ matrix.archive }}" >> "$GITHUB_OUTPUT"

      - name: Package artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/${{ matrix.binary }} dist/gitquarry
          cp README.md LICENSE dist/
          tar -C dist -czf "${{ steps.asset.outputs.name }}" .

      - name: Package artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/${{ matrix.binary }}" "dist/gitquarry.exe"
          Copy-Item "README.md" "dist/README.md"
          Copy-Item "LICENSE" "dist/LICENSE"
          Compress-Archive -Path "dist/*" -DestinationPath "${{ steps.asset.outputs.name }}" -Force

      - name: Upload packaged artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ matrix.target }}
          path: ${{ steps.asset.outputs.name }}

  publish-release:
    name: Publish release
    needs:
      - build-release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Download release artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: release-artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find release-artifacts -maxdepth 2 -type f -exec cp {} release/ \;

      - name: Generate release notes
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail

          VERSION="${VERSION#v}"
          awk -v version="$VERSION" '
            $0 == "## [" version "]" {
              in_section=1
              found_version=1
              next
            }
            !found_version && $0 == "## Unreleased" {
              in_section=1
              next
            }
            in_section && /^## / { exit }
            in_section { print }
          ' CHANGELOG.md > release/RELEASE_NOTES.md

          if [ ! -s release/RELEASE_NOTES.md ]; then
            echo "Failed to extract release notes for ${VERSION} or the Unreleased section from CHANGELOG.md" >&2
            exit 1
          fi

      - name: Create source tarball and checksums
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          git archive --format=tar.gz --output "release/gitquarry-${VERSION}-source.tar.gz" "${VERSION}"
          cd release
          sha256sum * > SHA256SUMS

      - name: Upload release bundle
        uses: actions/upload-artifact@v7.0.1
        with:
          name: gitquarry-release-bundle
          path: release/*

      - name: Upload release artifacts
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release upload "${GITHUB_REF_NAME}" release/* --clobber
            gh release edit "${GITHUB_REF_NAME}" \
              --title "${GITHUB_REF_NAME}" \
              --notes-file release/RELEASE_NOTES.md
          else
            gh release create "${GITHUB_REF_NAME}" release/* \
              --title "${GITHUB_REF_NAME}" \
              --notes-file release/RELEASE_NOTES.md \
              --verify-tag
          fi
